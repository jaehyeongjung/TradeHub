import { RealTimeSection } from "@/components/RealTimeSection";

export const Home = () => {
    return (
        <div className="min-h-screen">
            <RealTimeSection />
        </div>
    );
};

export default Home;
