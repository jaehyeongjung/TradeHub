// user_id에서 결정론적으로 뽑는 닉네임. 저장하지 않는다 —
// 같은 사람은 어느 방에서든 같은 이름으로 보이고, DB에 컬럼이 늘지 않는다.
//
// 32 × 32 = 1,024가지. 층 접두어가 앞에 붙어("9층 존버흑두루미") 실제로
// 헷갈리는 경우는 "같은 층 · 같은 닉네임"이 겹칠 때뿐이다.

const MODIFIERS = [
    "존버", "물타기", "새벽", "잠못든", "태연한", "초조한", "느긋한", "성실한",
    "무심한", "용감한", "노련한", "굳건한", "조용한", "담담한", "부지런한", "낙천적인",
    "비장한", "침착한", "우직한", "눈물의", "불꽃", "철벽", "오늘도", "십년째",
    "반포기", "버티는", "야근하는", "커피중독", "손절못한", "눈감은", "묵묵한", "느린",
] as const;

const CREATURES = [
    "흑두루미", "고라니", "너구리", "수달", "다람쥐", "오소리", "청둥오리", "왜가리",
    "부엉이", "올빼미", "산양", "노루", "담비", "물개", "해달", "까치",
    "딱따구리", "고등어", "참새", "두더지", "여우", "살쾡이", "반달곰", "멧돼지",
    "기린", "알파카", "라쿤", "펠리컨", "도롱뇽", "자라", "개구리", "물총새",
] as const;

/** FNV-1a 32비트. 짧은 uuid 문자열에 충분히 고르게 흩어진다. */
function hash32(input: string): number {
    let h = 0x811c9dc5;
    for (let i = 0; i < input.length; i++) {
        h ^= input.charCodeAt(i);
        h = Math.imul(h, 0x01000193) >>> 0;
    }
    return h >>> 0;
}

export function nicknameFor(userId: string): string {
    const h = hash32(userId);
    // 같은 해시의 다른 비트 구간을 써서 두 단어가 같이 움직이지 않게 한다
    return MODIFIERS[h % MODIFIERS.length] + CREATURES[(h >>> 11) % CREATURES.length];
}
